---
slug: insites/api/get-all-customers-no-company-data.json
searchable: false
metadata: {
  insites: "true"
}
authorization_policies:
  - modules/insites_core/insites_only_allowed_if_logged_in
  - modules/insites_core/insites_only_allowed_by_administrators
---

{% assign sortBy = "updated_at" %}
{% assign orderBy = "asc" %}

{% if params.sortBy %}
    {% assign sortBy = params.sortBy | downcase %}
{% endif %}
{% if params.orderBy %}
    {% assign orderBy = params.orderBy | downcase %}
{% endif %}
 
{% query_graph 'modules/insites_core/insites_get_all_customers_no_company_paginated', result_name: 'customer_list',  sortBy: sortBy, orderBy: orderBy %}
{{ customer_list | json }}